# TODO

## v0.0.5 — 规则引擎太多命令，用户不知道用什么

✅ 已完成：收敛为 audit / extract 两个入口，9 个底层命令隐藏。

## v0.0.6 — 报告写得像技术日志，业务专家看不懂

✅ 已完成：audit 报告按三组输出（需确认 / 平台发现 / 建议关注），extract 无 LLM 也能用。

## v0.0.7 — 报告只告诉有什么问题，不告诉怎么改

- [ ] **#22** audit 报告每个问题追加操作路径
  - 停止条件：每个问题项附带操作指引，例如 "→ 检查 data/xxx/domain.json 中的 files 字段"
  - 对引用断裂、JSON 错误、术语冲突分别写不同的操作模板

## v0.0.8 — 新手被空骨架的大量报错吓到

- [ ] **#23** 区分新建模式与正式模式
  - 停止条件：`audit --mode new` 只检查结构完整性（文件存在、JSON 合法），不检查内容质量（本体抽象度、覆盖率）
  - `audit --mode formal`（默认）保持当前严格检测

## v0.0.9 — 失败路径不一致 + extract 输出不像人话

- [ ] **#21** extract 输出改为一句话摘要
  - 停止条件：extract 完成后只输出 1–2 句话，例如 "抽取完成。新增 2 个领域（XX、YY），共收录 5 份文档。"
  - 详细匹配信息移至 `--verbose` 控制
- [ ] **#24** 统一失败路径处理
  - 停止条件：data_home 不存在、sample_home 未设置、JSON 文件缺失都给出清晰的业务语言错误
  - 测试覆盖上述 3 种失败路径

## v0.0.10 — 这次跑的和上次跑的看不出变化

- [ ] **#25** 审计报告加版本对比
  - 停止条件：audit 在 data_home 下写入 `.audit.json` 记录结果，下次运行时增量对比
  - 输出："相比上次审计（YYYY-MM-DD）：✅ 已修复 N 项 / 🆕 新增 N 项 / ⏳ 待处理 N 项"
  - `.audit.json` 不提交到 git，加入 .gitignore

## v0.10.0 — 业务专家还不能自助完成全流程

（待规划：LLM 语义抽取 + audit 质量门禁 → 文档入库到发布的完整闭环）
