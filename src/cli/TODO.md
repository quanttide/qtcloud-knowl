# TODO

## v0.0.6 — 面向任务的 CLI 重构

前置依赖：v0.0.5 agent 基础设施（`app/agents/` 子包，包含 engine、tools、llm 模块）。

### CLI 收敛

- [x] **#9** 新增 `qtcloud-knowl audit` 命令
  - 停止条件：对 fixtures 输出完整审计报告；空目录、单领域 JSON 错误等异常情况给出明确错误而非崩溃
  - [x] Agent 自动串行执行全部检测
  - [x] 聚合各检测结果，解释根因
  - [x] 输出标记 **【需人确认】**
  - [x] 输出一份可读审计报告
  - [x] 异常处理：空目录 → 输出"领域数量: 0"
  - [x] 异常处理：JSON 格式错误 → 捕获异常，标记对应检测为"警告"
  - [x] 全部检测失败 → `all_pass` 标志控制最终结果
  - [x] 测试覆盖正常路径和异常路径（`tests/test_audit.py`）
  - 超出能力（标记 **【需人确认】**）：
    - 检测出术语冲突 → 智能体列出冲突，由人决定保留/重命名/合并
    - 引用断裂涉及模糊名称 → 智能体列出候选，由人确认目标文件
    - `sample_home` 未设置且未传参 → 打印错误并退出
- [x] **#10** 新增 `qtcloud-knowl extract` 命令
  - 停止条件：对 fixtures input 执行 `extract`，异常输入不导致静默失败
  - [x] 输入：源文档目录
  - [x] Agent 自动执行完整知识发现流程（领域→本体→实例→关系→跨域融合）
  - [x] 输出：填充完成的 domain/ontologies/instances/relations JSON
  - [x] 异常：源文档目录不存在 → 打印错误并退出
  - [x] 异常：源文档空目录 → 输出空知识库骨架并提示"无内容"
  - [x] 异常：LLM API key 未配置 → 打印提示并退出
  - [x] 测试覆盖正常路径和异常路径（`tests/test_extract.py`）
  - 超出能力（标记 **【需人确认】**）：
    - 领域视角选择 → 智能体推荐最佳匹配视角，由人确认或切换（需 LLM 支持）
    - 本体抽象度是否达标 → 按 `docs/criteria.md` 自检（需 LLM 支持）
    - 实例内容准确性 → 智能体标注"AI 抽取"，由人审核修正（需 LLM 支持）
    - 跨领域关系真实性 → 智能体推荐候选关系，由人确认是否存在（需 LLM 支持）
    - 本体/实例 ID 和命名 → 智能体按约定生成建议名，由人确认（需 LLM 支持）

### 底层 API 降级

- [x] **#11** 将当前 9 个命令的 Typer 注册方式改为不公开 / 仅 `--help` 隐藏
  - 停止条件：`qtcloud-knowl --help` 只显示 `audit` 和 `extract`；测试中仍可通过 CliRunner 直接调用底层命令
  - [x] 所有 9 个命令标记 `hidden=True`
  - [x] `--help` 输出只显示 `audit` 和 `extract`
  - [x] 测试验证公开命令可见、隐藏命令不可见

### 文档与测试

- [x] **#12** 更新 README.md CLI 使用说明
  - 停止条件：README 中 CLI 章节只描述 `audit` 和 `extract`，不含底层命令
- [x] **#13** 更新 STATUS.md
  - 停止条件：STATUS.md 工具链表格包含 `audit` 和 `extract`，不列底层命令
- [x] **#14** 测试 `audit` 命令（集成测试）
  - 停止条件：测试覆盖正常路径（对 fixtures 输出报告）和异常路径（空目录、JSON 格式错误）
- [x] **#15** 测试 `extract` 命令（集成测试）
  - 停止条件：测试覆盖正常路径（空目录）、异常路径（不存在目录、无 API key）

---

## 存量问题

- [ ] **#8** fusion-check 引用文件 `《量潮数据项目岗位权责章程》` 不存在
  - 停止条件：确认该文件是否应存在于 SAMPLE_DIR 中，或是引用错误。若是引用错误则修复或标记忽略。
