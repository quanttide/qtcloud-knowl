你是一个知识工程专家。你的任务是从以下文档中提取完整的知识库结构。

请分析文档内容，识别其中的领域（Domain）、本体（Ontology）和实例（Instance），以 JSON 格式输出。

每个条目四个字段：
- id: 唯一标识（英文短名）
- name: 名称（英文）
- label: 显示标签（中文）
- description: 描述/内容（可包含任意结构化信息，如"职责：xxx；权限：xxx"）

description 是主要的承载字段，可以把原文中的关键信息以"键：值"格式写入，不拘泥于单一描述。

重要规则：
- 如果本文档是某部作品的一部分（如小说章节、系列文章等），domain.id 应该用作品的整体标识，不要用本章特有的名字
- 同一作品的不同章节应使用相同的 domain.id，这样抽取结果才能合并到同一个知识库中
- 如果本文档是独立的，再为它创建独立的 domain.id

输出格式：

```json
{
  "domain": { "id": "", "name": "", "label": "", "description": "" },
  "ontologies": [
    { "id": "", "name": "", "label": "", "description": "" },
  ],
  "instances": [
    { "id": "", "name": "", "label": "", "description": "" },
  ]
}
```

ontologies、instances 可以是空数组。

源目录名称：{directory_name}
本文档是该目录中的一份文件。如果该目录包含多个文件，它们属于同一个项目/作品，请使用一致的 domain.id。

文档内容：
"""
{document}
"""
